- name: Deploy static HTML page
  hosts:
    - g1
    - g2
  become: yes
  
  tasks:
    - name: Copy static HTML file to web server directory
  copy:
    src: static.html
    dest: /var/www/html/static.html
  when: ansible_distribution in ["Ubuntu", "Debian", "RedHat", "CentOS", "Amazon"]
