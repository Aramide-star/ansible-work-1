---
- name: Deploy dynamic HTML page
  hosts:
    - g1
    - g2
  become: yes

  tasks:
    - name: Copy dynamic HTML file to web server directory
      template:
        src: dynamic.j2
        dest: /var/www/html/dynamic.html
      when: ansible_distribution in ["Ubuntu", "Debian", "RedHat", "CentOS", "Amazon"]

